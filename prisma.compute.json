{
  "region": "us-east-1",
  "app": {
    "name": "severfastify",
    "framework": "custom",
    "httpPort": 3000,
    "build": {
      "command": "npm run build",
      "outputDirectory": "dist",
      "entrypoint": "server.js"
    }
  }
}
